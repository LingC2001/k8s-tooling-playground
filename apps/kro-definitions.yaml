apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kro-definitions
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/LingC2001/k8s-tooling-playground
    targetRevision: HEAD
    path: definitions
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true